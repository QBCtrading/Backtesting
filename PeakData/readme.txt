The files here are for version control only. Peak does not load the files from this location.
    // each line contains a date in serial number, a comma, and some comments that we don't care.
    // for example:
    // 42005,20150101
    // the dates must be sorted.
    // the first line can optionally define a projection start date
    // # projection_start_date = 42369
